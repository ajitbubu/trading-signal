# Core
streamlit==1.39.0
pandas==2.2.3
numpy==2.1.3
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1
structlog==24.4.0
PyYAML==6.0.2

# Market data
yfinance==0.2.50
nsepython==2.95
# pandas_ta intentionally omitted: we implement RSI manually with Wilder
# seeding (DECISIONS.md D-010) because pandas_ta's pre-release versions
# fail to install on Python 3.11 by default.

# Networking / async / caching
httpx==0.27.2
diskcache==5.6.3
tenacity==9.0.0

# Persistence
SQLAlchemy==2.0.36

# News ingestion
# feedparser + trafilatura land with the RSS adapter (post-v1).
# feedparser==6.0.11
# trafilatura==1.12.2
rapidfuzz==3.10.1
vaderSentiment==3.3.2

# Scheduling
APScheduler==3.10.4

# Optional LLM (gated by feature flag)
anthropic==0.39.0

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.4
